package com.example.firsttest.ui.streak

import androidx.compose.runtime.Composable
import androidx.compose.ui.Modifier
import com.example.firsttest.ui.common.PlaceholderScreen

/**
 * 每日连胜 — streak calendar + check-in (2.4.1).
 *
 * Phase 2 placeholder.
 */
@Composable
fun StreakScreen(modifier: Modifier = Modifier) {
    PlaceholderScreen(
        title = "每日连胜",
        subtitle = "连胜打卡 (2.4.1) — Phase 2",
        modifier = modifier,
    )
}
